apiVersion: 1

providers:
  - name: "IoT Platform Dashboards"
    orgId: 1
    folder: "IoT Platform"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
